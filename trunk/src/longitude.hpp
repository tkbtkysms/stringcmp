#include<iostream>
#include<vector>

typedef unsigned int uint;
typedef unsigned char uchar;

class longitude{
private:
  std::vector<uchar> variable;  
  uint size;
public:
private:
public:
    void setvariable(uchar newvariable);
    char getvariable(int num);
    uint getsize();
  ~longitude();
  longitude();
};
